/**
 * CONFIG
 * 
 * app-wide configuration for non-sensitivie data
 *
 * note: sensitive data should be stored within `.env` or within
 * your hosting envs ENV value configuration tool and accessed
 * via process.env
 *
 */
const config = {
    // EXAMPLE_CONFIG_KEY: {

    // },
};

module.exports = config;